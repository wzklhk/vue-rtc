// 需要在index.html中引入js
// eslint的globals中需添加变量否则会报红
// eslint-disable-next-line no-unused-vars
const CONFIG = {
  url: "https://127.0.0.1:23333/",
  wsUrl: "wss://127.0.0.1:23333/",
};

const MODULES = {
  jpa: "jpa",
  im: "im",
};
